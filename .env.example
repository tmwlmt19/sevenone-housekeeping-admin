# Local defaults below. Per-env values are set in Vercel (Production + Staging).
# See the web repo's docs/environments.md.
VITE_API_BASE_URL=http://localhost:8000
VITE_LOGIN_URL=http://localhost:5174
